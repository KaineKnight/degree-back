import { ValidateCreateUsersPipe } from './validate-create-users.pipe';

describe('ValidateCreateUsersPipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateUsersPipe()).toBeDefined();
  });
});
