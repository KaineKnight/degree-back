export type MinMax = {
  maxPrice: number;
  minPrice: number;
  maxTime: number;
  minTime: number;
  maxBrand: number;
  minBrand: number;
  maxCategory: number;
  minCategory: number;
  maxCommonness: number;
  minCommonness: number;
};
